defmodule Baiji.Generator.Spec.Writer do
  
  @doc """
  Given a list of specs, write generated .ex files for each
  service to the path specified
  """
  def write(specs, path) when is_list(specs) do
    
  end
end